# YearnPass Marketplace — Admin Ready (Updated)
- Users can **Buy** passes (Marketplace.buy)
- Admins (with MINTER_ROLE) can **Mint** and **Transfer** passes

## Setup
```bash
npm i
cp .env.example .env
npm run dev
```

